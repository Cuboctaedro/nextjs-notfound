import { NotFoundContent } from "@/components/not-found-content";

const NotFoundPage = () => {
    return (
        <div id="not-found">
            <NotFoundContent />
        </div>
    );
};

export default NotFoundPage;
